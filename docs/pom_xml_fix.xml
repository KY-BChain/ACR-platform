<!-- 
   ADD THIS DEPENDENCY TO pom.xml
   Location: ~/DAPP/ACR-platform/microservices/openllet-reasoner/pom.xml
   
   Insert this in the <dependencies> section, after Spring Boot dependencies
-->

<dependency>
    <groupId>javax.annotation</groupId>
    <artifactId>javax.annotation-api</artifactId>
    <version>1.3.2</version>
</dependency>

<!-- 
   This fixes the 9 compilation errors:
   - @PostConstruct annotation
   - @PreDestroy annotation
   
   After adding, run:
   cd ~/DAPP/ACR-platform/microservices/openllet-reasoner
   mvn clean compile
-->
